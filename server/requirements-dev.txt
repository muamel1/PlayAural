# Server runtime plus development/test dependencies for local Windows fallback setup.
# Keep this in sync with pyproject.toml project dependencies and the dev extra.
websockets>=12.0
mashumaro>=3.11
fluent-runtime>=0.4.0
babel>=2.14
openskill>=6.1.3
argon2-cffi>=23.1
pytest>=8.0
pytest-asyncio>=0.23
pytest-xdist>=3.5
